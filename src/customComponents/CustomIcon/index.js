import {createIconSetFromIcoMoon} from 'react-native-vector-icons';
import iconConfig from '../../../selection.json';

const Icon = createIconSetFromIcoMoon(iconConfig, 'icomoon', 'icomoon.ttf');

export default Icon;
